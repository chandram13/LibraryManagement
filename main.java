// Marvish Chandra

public static void registeredBooks(string[] name,book,issuedDays,afterExpiration){
    String givenName = name;
    System.out.println(givenName.toUpperCase());
    String rentedBook = book;
    System.out.println(rentedBook.toUpperCase());
    int givenDays = issuedDays;
    System.out.println(issuedDays);
    int nonreturnedDays = afterExpiration;
    System.println(nonreturnedDays);
}